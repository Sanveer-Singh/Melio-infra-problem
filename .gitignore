/build/
target
classes
checkouts
pom.xml
pom.xml.asc
*.jar
*.class
.lein-*
.nrepl-port
.hgignore
.hg/
*.swp
.DS_Store
*.iml
.idea/

# Terraform
*.tfstate
*.tfstate.backup
.terraform/
terraform.tfvars
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# NOTE: .terraform.lock.hcl is intentionally NOT ignored -- commit it for reproducible provider installs

# SSH keys
*.pem

# Cursor IDE (plans are local; rules are shared)
.cursor/plans/
